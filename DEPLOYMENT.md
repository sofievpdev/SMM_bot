# 🚀 Развёртывание на Render

## Быстрый старт

### 1. Подготовка окружения

Скопируй `.env.example` в `.env` и заполни все переменные:

```bash
cp .env.example .env
```

### 2. Обязательные переменные окружения

```bash
# Telegram Bot (получи от @BotFather)
TELEGRAM_BOT_TOKEN=<твой_токен>

# Telegram API (с https://my.telegram.org/apps)
TELEGRAM_API_ID=<id>
TELEGRAM_API_HASH=<hash>
TELEGRAM_PHONE=<номер_телефона>

# Claude AI API Key (от Anthropic)
CLAUDE_API_KEY=<твой_ключ>

# SMM.media API (для бустинга метрик)
SMM_MEDIA_API_KEY=<твой_ключ>
SMM_MEDIA_SERVICE_ID=<id_услуги>

# Расписание публикаций (Cyprus timezone UTC+2)
PUBLISH_TIMES=["08:00","19:00"]

# Окружение
NODE_ENV=production
```

## Развёртывание на Render

### 1. Создание сервиса

1. Перейди на [render.com](https://render.com)
2. Нажми "New +" → "Web Service"
3. Выбери GitHub репозиторий
4. Заполни настройки:
   - **Name:** `smm-bot`
   - **Environment:** `Node`
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
   - **Plan:** Free или Paid (зависит от нужной мощности)

### 2. Добавление Environment Variables

В панели Render добавь все переменные из `.env`:

```
TELEGRAM_BOT_TOKEN=...
TELEGRAM_API_ID=...
TELEGRAM_API_HASH=...
TELEGRAM_PHONE=...
CLAUDE_API_KEY=...
SMM_MEDIA_API_KEY=...
SMM_MEDIA_SERVICE_ID=...
PUBLISH_TIMES=["08:00","19:00"]
NODE_ENV=production
```

### 3. Deploy

1. Нажми "Deploy"
2. Жди ~5 минут пока сервис запустится
3. Проверь логи в разделе "Logs"

## Автоматический деплой

После каждого `git push` в main:
1. GitHub отправляет вебхук на Render
2. Render автоматически перестраивает сервис
3. Новая версия кода загружается ~3-5 минут

## Логи и мониторинг

Просмотри логи в Render панели:
- **Live Logs:** Текущие события
- **Logs:** История
- **Events:** Развёртывания и ошибки

Ищи эти сообщения успеха:
```
✓ Bot initialized successfully
✓ Schedule initialized: 0 8 * * * (Cyprus timezone - 08:00)
✓ Schedule initialized: 0 19 * * * (Cyprus timezone - 19:00)
```

## Проблемы и решения

### Сервис не стартует
```
1. Проверь все env переменные заполнены
2. Посмотри логи ошибок
3. Убедись что все зависимости установлены: npm install
```

### Посты не публикуются
```
1. Проверь что канал @sofismm22 активен
2. Убедись что токены Telegram верные
3. Посмотри логи: "Starting publish cycle..."
4. Проверь что текущее время совпадает с PUBLISH_TIMES
```

### Переводы не работают
```
1. Убедись что CLAUDE_API_KEY правильный
2. Проверь квоту на Claude API (у тебя достаточно?)
3. Посмотри логи: "Translating articles..."
```

### Парсинг не работает
```
1. Проверь что сайты доступны (не заблокированы)
2. Попробуй перезагрузить: git push
3. Посмотри логи: "Scraping content for theme..."
```

## Ручные команды

### Принудительная публикация

Добавь это в Render "Run Shell Command":
```bash
curl -X POST https://twoj-sms-bot.onrender.com/api/publish
```

Или через SSH:
```bash
ssh -i key.pem user@host "cd /app && npm run dev"
```

## Мониторинг производительности

### Проверь используемые ресурсы
- **CPU:** Должна быть < 50% в нормальных условиях
- **Memory:** Должна быть < 500MB (Free plan)
- **Disk:** Нет проблем (не пишем большие файлы)

### Оптимизация

Если используется много ресурсов:
1. Уменьши частоту скрейпинга (пока стоит 08:00 и 19:00 - оптимально)
2. Кэшируй результаты парсинга
3. Используй Paid план на Render

## Обновление кода

```bash
# 1. Сделай изменения локально
git add .
git commit -m "описание изменений"

# 2. Пушни в GitHub
git push origin main

# 3. Render автоматически перестроит через ~5 минут
```

## Полезные ссылки

- [Render Dashboard](https://render.com/dashboard)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Telegram Client API](https://core.telegram.org/methods)
- [Claude API Documentation](https://docs.anthropic.com)
- [SMM.media API](https://smm.media/api)

## Резервная копия логов

Render сохраняет логи только ~1 месяц. Если нужна история:
1. Экспортируй логи через Render API
2. Сохрани в GitHub Issues
3. Или настрой логирование в облаке (CloudFlare, Datadog)

## Отключение сервиса

Если нужно остановить бота временно:
1. Перейди на Render Dashboard
2. Нажми "Suspend" на сервисе
3. Бот не будет публиковать посты
4. Нажми "Resume" чтобы включить обратно

---

✅ **Готово к развёртыванию!**

Бот полностью интегрирован и готов работать на Render. После развёртывания он будет:
- Парсить статьи из 9 источников
- Переводить их на русский
- Генерировать профессиональные посты
- Публиковать 2 раза в день (08:00 и 19:00 Cyprus Time)
- Публиковать 14 постов в неделю автоматически
