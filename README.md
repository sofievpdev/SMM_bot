# 🤖 SMM Bot - Telegram Content Generator

Автоматизированный бот для управления Telegram каналами. Генерирует контент через Claude AI и публикует по расписанию.

## 🎯 Функциональность

✅ **Генерация контента** - Claude AI создаёт оригинальные посты
✅ **Автоматическая публикация** - По расписанию в несколько каналов
✅ **Настраиваемые промпты** - Редактируй system prompt для каждого канала
✅ **Бустинг метрик** - Интеграция с SMM.media для повышения просмотров

## 📁 Структура

```
SMM_bot/
├── config/config.js         # System prompts для каналов (РЕДАКТИРУЙ ТУТ!)
├── services/
│   ├── ai-generator.js      # Claude API
│   ├── publisher.js         # Публикация в Telegram
│   ├── metrics-booster.js   # SMM.media интеграция
│   └── telegram-parser.js   # Парсинг контента
├── handlers/schedule.js     # Шедулер (23:15 Cyprus TZ)
├── bot.js                   # Главный файл
├── .env                     # Твои ключи (не коммитить!)
└── package.json
```

## 🚀 Установка и запуск

### 1️⃣ Установи зависимости
```bash
cd ~/Desktop/SMM_bot
npm install
```

### 2️⃣ Настрой .env файл

В файле `.env` уже указаны твои ключи:
- ✅ Telegram Bot Token
- ✅ Claude API Key
- ✅ SMM.media API Key
- ⚠️ API_ID и API_HASH (если нужен парсинг)

### 3️⃣ Отредактируй System Prompts

Открой **[config/config.js](config/config.js)** и измени `systemPrompt` для каждого канала:

```javascript
export const channels = {
  medicine: {
    name: '@sofismm22',
    systemPrompt: `Ты - профессиональный медицинский контент-райтер...` // ← РЕДАКТИРУЙ
  },
  nutrition: {
    name: '@sofiesmm',
    systemPrompt: `Ты - профессиональный нутрициолог...` // ← РЕДАКТИРУЙ
  }
};
```

### 4️⃣ Запусти бота
```bash
npm start
```

**Бот будет публиковать каждый день в 23:15 (Cyprus Time)**

## 🔧 Использование

### Ручная публикация (для тестирования)

Отредактируй `bot.js`:
```javascript
// Замени это:
setTimeout(async () => {
  await publishNow();
}, 5000);

// На это:
await publishNow();  // Публикация сразу при запуске
```

### Смена времени публикации

Изменить в `.env`:
```env
PUBLISH_TIME=14:30  # Новое время в формате HH:MM
```

### Добавить новый канал

В `config/config.js` добавь:
```javascript
export const channels = {
  // ... существующие
  newChannel: {
    name: '@new_channel',
    type: 'topic',
    enabled: true,
    systemPrompt: `Ты - контент-райтер о ...`
  }
};
```

## 📊 API Services

### Генерация контента
```javascript
import { generateFromIdea } from './services/ai-generator.js';
const post = await generateFromIdea('идея', systemPrompt);
```

### Публикация
```javascript
import { publishToMultiple } from './services/publisher.js';
await publishToMultiple(['@channel1', '@channel2'], 'текст');
```

### Бустинг метрик
```javascript
import { boostMetrics } from './services/metrics-booster.js';
await boostMetrics('https://t.me/@channel/123', 'tg_post_views', 100);
```

## 🐛 Troubleshooting

| Проблема | Решение |
|----------|---------|
| "Failed to generate content" | Проверь Claude API ключ в .env |
| "Failed to publish" | Убедись что бот - администратор в каналах |
| "Model not found" | Проверь имя модели в ai-generator.js |
| Бот не запускается | Запусти `npm install` ещё раз |

## 📝 Примеры System Prompts

### Для медицины (@sofismm22)
```
Ты - профессиональный медицинский контент-райтер для Telegram.
Создавай позитивный контент о медицине и здоровье.
- Пиши позитивно о возможностях медицины
- Добавляй интересные факты
- Размер: 150-300 символов
- Тон: дружеский, информативный
- Завершай призывом к действию
```

### Для питания (@sofiesmm)
```
Ты - профессиональный нутрициолог для Telegram.
Создавай вдохновляющий контент о правильном питании.
- Фокусируйся на пользе и вкусе
- Включай практические советы
- Размер: 150-300 символов
- Тон: позитивный, практичный
- Используй реальные примеры
```

## 🔗 Ссылки

- [Telegram Bot API Docs](https://core.telegram.org/bots/api)
- [Claude API Docs](https://docs.anthropic.com)
- [SMM.media API](https://smm.media)

## 💡 Tips

✨ Бот использует **mock данные** для парсинга - ты можешь заменить на реальный парсинг позже
✨ Все логи выводятся в консоль с временем и эмодзи
✨ Бот gracefully shutdownится при Ctrl+C

## 📧 Support

Вопросы? Проверь логи при запуске или открой Issue на GitHub!
