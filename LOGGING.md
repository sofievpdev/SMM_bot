# 📊 Система логирования

## 🚀 Как это работает

Система автоматически:
1. **Логирует все события** в консоль И в файлы
2. **Сохраняет логи** в папке `logs/` с именем `bot-YYYY-MM-DD.log`
3. **Автоматически чистит** старые логи (>7 дней)
4. **Предоставляет API** для просмотра логов через веб-интерфейс

## 📈 API эндпоинты

**Когда бот запущен**, он запускает сервер логов на порту 3000:

### 1. Получить последние логи
```bash
curl http://localhost:3000/api/logs
```
Возвращает последние 200 логов

Параметры:
- `?limit=100` — получить последние 100 логов

Пример:
```bash
curl http://localhost:3000/api/logs?limit=50
```

### 2. Получить только ошибки
```bash
curl http://localhost:3000/api/logs/errors
```
Возвращает только логи с `[ERROR]`

Параметры:
- `?limit=50` — получить последние 50 ошибок

### 3. Получить логи за конкретный день
```bash
curl http://localhost:3000/api/logs/2026-02-08
```
Возвращает все логи за день 2026-02-08

## 📁 Файлы логов

**Расположение:** `/logs/`

**Формат:** `bot-YYYY-MM-DD.log`

Пример содержимого:
```
[2026-02-08T15:30:45.123Z] [INFO] 📝 Generating content from idea: Создай кейс пациента
[2026-02-08T15:30:50.456Z] [SUCCESS] ✅ Content generated successfully
[2026-02-08T15:31:00.789Z] [ERROR] ❌ Failed to connect to SMM.media API
```

## 🔍 Как читать логи

### На локальной машине
```bash
# Смотреть логи за сегодня
tail -f logs/bot-2026-02-08.log

# Смотреть последние 50 строк
tail -n 50 logs/bot-2026-02-08.log

# Поиск ошибок
grep ERROR logs/bot-2026-02-08.log
```

### На Render.com
1. Зайди в dashboard Render
2. Выбери свой сервис `smm-bot`
3. Нажми на "Logs" в top-right
4. Посмотри логи в реальном времени

Или используй API:
```bash
# С ноутбука/за границей
curl https://your-smm-bot.onrender.com/api/logs
curl https://your-smm-bot.onrender.com/api/logs/errors
```

## 🛠️ Автодеплой и мониторинг

### Автодеплой включен ✅
- При каждом коммите в `main` → бот автоматически перезагружается на Render
- `render.yaml` содержит: `autoDeploy: true`

### Мониторинг ошибок
При ошибке в логе будет:
```
[ERROR] ❌ SMM.media API connection failed
```

Это означает:
- ❌ API ключ неправильный
- ❌ Нет интернета
- ❌ SMM.media выключен
- ❌ Лимит запросов превышен

## 📋 Примеры логирования

### Успешная публикация
```
[INFO] 🚀 Starting publish cycle
[INFO] 📅 Current day: monday
[INFO] 📢 Processing channel: @sofismm22
[INFO] 🌐 Scraping content for theme: oncology
[SUCCESS] ✅ Scraped 2 articles
[INFO] 🔄 Translating articles
[SUCCESS] ✅ Translated 2 articles
[SUCCESS] ✅ Post published: @sofismm22/123
[SUCCESS] ✓ Reactions boost order created: #1234567
[SUCCESS] ✓ Views boost order created: #1234568
```

### Ошибка при публикации
```
[ERROR] ❌ Failed to generate content: 401 Unauthorized
[ERROR] HTTP Status: 401
[ERROR] SMM.media Error: Invalid API key
```

## 🔧 Отладка проблем

**Проблема:** Посты не публикуются
1. Смотри API ошибки:
   ```bash
   curl https://your-app.onrender.com/api/logs/errors
   ```
2. Ищи `[ERROR]` в логах
3. Проверь API ключи в переменных окружения

**Проблема:** Просмотры не добавляются
1. Смотри логи SMM.media:
   ```bash
   curl https://your-app.onrender.com/api/logs | grep "SMM.media"
   ```
2. Проверь баланс на SMM.media
3. Проверь service_id = 821

**Проблема:** Картинки не добавляются
1. Смотри Unsplash ошибки:
   ```bash
   curl https://your-app.onrender.com/api/logs | grep "image"
   ```
2. Проверь UNSPLASH_ACCESS_KEY в переменных

## 💡 Советы

1. **Для отладки** в dev режиме:
   ```bash
   npm run dev
   # Посмотреть логи + перезагрузка при изменении файла
   ```

2. **Интеграция с мониторингом:**
   - Можешь написать скрипт который проверяет `/api/logs/errors` каждый час
   - Если есть ошибки → отправить уведомление в Telegram

3. **Архивирование логов:**
   - Логи автоматически удаляются через 7 дней
   - Если нужны долгие архивы → давай модифицируем

## 🚀 На Render

После деплоя:
- `render.yaml` автоматически используется
- `autoDeploy: true` срабатывает при каждом коммите
- Логи доступны в реальном времени:
  - Через Render Dashboard → Logs
  - Через API: https://your-smm-bot.onrender.com/api/logs

